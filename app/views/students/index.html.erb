
<br/>

	<ol>
    
	<% @students.each do |student| %>
 

<li>  <%= link_to student.name,student %></li>  


 <% if can? :update, student %><%= link_to 'Edit', edit_student_path(student) %><%end %>
  <% if can? :destroy, student %><%= link_to 'Destroy', student, method: :delete, data: { confirm: 'Are you sure?' } %><% end %>
  
   
    <% end %> 
 
</ol>

<% if can? :create, Student %>
<%= link_to 'New Student', new_student_path(@student) %><br>
<% end %>


